import { Dashboard } from "@/components/Dashboard";

/** Console fixo em modo operador — ideal para TV na linha de produção. */
export default function OperadorPage() {
  return <Dashboard kiosk />;
}
